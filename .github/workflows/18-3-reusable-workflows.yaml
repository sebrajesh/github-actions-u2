name: 18 - 3 - Reusable Workflows

on: 
  workflow_dispatch

jobs:
  deploy:
    uses: ./.github/workflows/18-1-reusable-workflows.yaml
    target-directory: react-app

  e2e-tests:
    needs: deploy
    uses: sebrajesh/github-actions-example-e2e/.github/workflows/e2e.yaml@main
